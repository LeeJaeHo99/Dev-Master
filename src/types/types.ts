export type cssPracticeState = {
    items: string;
}

export type clickCss = {
    css: string;
}

export interface tableData {
    sort: string;
    text: string;
    date: string;
    writer: string;
}